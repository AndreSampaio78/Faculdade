#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(){
	char texto[30], l, r;
	int i, tam, con;
	printf("Digite um texto qualquer: ");
	gets (texto);
	printf("%s", texto);
	tam=strlen(texto);
	for (i=0;i<=tam;i++){
		if(texto[i]==l) cont++;
		else{
			r==l
		}
	}
	
	
	return 0;
}
